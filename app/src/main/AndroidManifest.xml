<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yyf.camerascandemo">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".systemcameras.SystemCameraForThumbnails" />
        <activity android:name=".photoscan.PhotoScanActivity" />
        <activity android:name=".scanphoto.ScanPhotoActivity" />
        <activity android:name=".scanphoto.CameraActivity" />
        <activity android:name=".systemcameras.SystemCameraForOriginalImage" />
        <activity android:name=".countdownpictures.CountdownPictures" />
        <activity android:name=".customcamera.CustomCameraActivity"></activity>
        <activity android:name=".customcamera.CustomCameraActivity1"></activity>
    </application>

</manifest>